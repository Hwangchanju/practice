package com.spring.practice.controller;

public class trash {

	/*
	 * reqData.forEach((key, value)->{ if(!reqData.containsKey(key)) {
	 * reqData.remove(key); } });
	 * 
	 * List<SelectTableVO> resultList = stService.getSelectList(reqData);
	 * 
	 * System.out.println("resultList  ::::::" + resultList.toString());
	 */
	
}
