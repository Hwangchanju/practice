<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->		

	<!-- spring JDBC -->
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DrvierManagerDataSource">
		<property name="driverClassName" value="oracle.jdbc.OracleDriver" />
		<property name="url" value="jdbc:oracle:thin:@127.0.0.1:1521:xe" />
		<property name="username" value="week" />
		<property name="password" value="week" />
	</bean>
	
	<!-- spring과 mybatis 연결, mybatis 매퍼와 환경 설정 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<!-- setter()를 통한 DI 의존성 주입 -->
		<property name="dataSource" ref="dataSource" />
		<!-- mybatis-config.xml 파일을 읽어옴, 이 파일의 기능은 데이터 저장 빈 클래스의 객체명 별칭을 관리 -->
		<property name="configLocation" value="classpath:/mybatis-config.xml" />
		<!-- 쿼리문을 다루는 매퍼 태그를 포함한 xml 파일 위치 -->
		<property name="mapperLocations" value="classpath:/mappers/*.xml" />
	</bean>
	
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
	<!-- destroy-method="clearCache"는 임시 메모리를 비워 데이터베이스의 연결을 해제함 -->
	<constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory" />
	<!-- constructor-arg는 생성자를 통한 의존성 주입 -->
	</bean>

</beans>
